info:
  name: "Get Organizer "
  type: http
  seq: 2

http:
  method: GET
  url: zentry-backend.vercel.app/api/v1/organizer/:pulse-events
  params:
    - name: pulse-events
      value: pulse-events
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
